import 'package:flutter/material.dart';

class CardValidation extends StatelessWidget {
  const CardValidation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Card Validation'),
      ),
      body: const Center(
        child: Text('Card Validation Screen'),
      ),
    );
  }
}